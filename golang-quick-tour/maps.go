package main

import "fmt"

type values struct {
	s1, s2 string
}

func main() {

	// m := make(map[int]string, 10)

	// m[1] = "Alok"
	// m[2] = "Autage"
	// m[3] = "Atul"

	// if value, found := m[1]; found {
	// 	fmt.Println(found, value)
	// }
	/*
		// Creating Map of strings array

		m := make(map[int][]string)
		m[1] = append(m[1], "Alok")
		m[1] = append(m[1], "Atul")
		fmt.Println(m)

		// Creating logical type with GO

		m := make(map[int]values)
		m[1] = values{"Alok", "Atul"}
		fmt.Println(m)

	*/

}
